Обработка текстового файла TextFile.txt
осуществляется методами класса
wwv.
 
set_N 
	задает максимальное количество строк
	в выходном html файле

get_TextFile_lineCount() 
	возращает количество строк
	в исходном файле с текстом TextFile.txt

get_VocabFile_lineCount()
	возращает количество строк
	в файле со словарем VocabFile.txt

get_transformed_line(string[] words)
	осуществляет преобразование 
	считанной строки из файла в соответствии с требованиями
	задания

set_vocabulary()
	загружает словарь в память целиком

process_text_file ()
	осуществляет построчное чтение исходного
	файла и запись в html файл